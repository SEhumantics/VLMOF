# VL-MOF

An EMOF formalization project in Lean 4. This checkout currently contains the
development environment; the formalization has not started.

Install Lean through elan, then run `lake build`. The toolchain is pinned in
`lean-toolchain`. No external Lean libraries are required yet.
